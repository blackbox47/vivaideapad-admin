# Shared local-dev defaults — loaded for ALL modes but GITIGNORED.
# Points at the live NestJS API. For mock-mode offline dev, create
# environment/.env.localdev.local with VITE_USE_MOCK_API=true.
VITE_API_BASE_URL=http://localhost:3000/api/v1
VITE_USE_MOCK_API=false