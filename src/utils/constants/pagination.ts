/**
 * Shared pagination defaults across tables and overview views.
 */
export const DEFAULT_PAGE = 1;
export const DEFAULT_PAGE_SIZE = 6;
export const DEFAULT_API_PAGE_SIZE = 10;
export const DEFAULT_PAGE_SIZE_OPTIONS = [6, 12, 24, 48] as const;
